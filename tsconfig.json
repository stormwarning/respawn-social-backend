{
	"//": "On Deno, type-checking is done by `deno check` (see the `typecheck` task), which understands Deno globals and resolves npm/.js imports. This tsconfig exists only so editors and oxlint can parse the TypeScript syntax consistently; Deno itself reads compilerOptions from deno.json, not here.",
	"compilerOptions": {
		"target": "ESNext",
		"module": "ESNext",
		"moduleResolution": "Bundler",
		"lib": ["ESNext", "DOM", "DOM.Iterable"],
		"types": [],
		"noEmit": true,
		"strict": true,
		"noUncheckedIndexedAccess": true,
		"noImplicitOverride": true,
		"esModuleInterop": true,
		"resolveJsonModule": true,
		"skipLibCheck": true,
		"allowImportingTsExtensions": true
	},
	"include": ["src/**/*.ts", "*.config.ts"],
	"exclude": ["node_modules"]
}
